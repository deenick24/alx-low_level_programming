#include "main.h"
/**
 * mul - multiplies two integers
 * @a: character to be multiplied
 * @b: character to be multiplied
 * Return: return multiplied number
 */

int mul(int a, int b)
{
	return (a * b);
}
